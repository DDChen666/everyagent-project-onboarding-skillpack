# Git Checkpoint Checklist

Before Risk 2+ changes:

```bash
git status
git branch --show-current
git diff --stat
```

Preferred branch:

```bash
git checkout -b ai-agentification/<topic>
```

If working tree is dirty:

- identify user changes
- do not mix unrelated changes
- ask before proceeding with Risk 2+
